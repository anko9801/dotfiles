# Git SSH Allowed Signers
# This file lists the SSH keys that are allowed to sign commits
# Format: email key-type key-data

# Your signing key from 1Password (will be populated dynamically)
# Note: Run `ssh-add -L` to get your current 1Password SSH public key
# Then add it here in the format: email key-type key-data
{{ YADM_EMAIL }} ssh-ed25519 YOUR_1PASSWORD_PUBLIC_KEY_HERE