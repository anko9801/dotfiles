# yadm configuration

# Template variables for .gitconfig generation
# These should be set by each user after cloning
yadm.user = "Daiki Usami"
yadm.email = "d_usami@no9.co.jp"

# Auto-update submodules
yadm.auto-submodules = true

# Enable git-crypt if available
yadm.git-crypt = true

# Git settings
yadm.git-dir = "~/.local/share/yadm/repo.git"