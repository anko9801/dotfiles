[user]
    name = {{ yadm.user }}
    email = {{ yadm.email }}

[core]
    editor = vim
    pager = less

[color]
    ui = auto

[alias]
    st = status
    co = checkout
    br = branch
    ci = commit
    di = diff
    lg = log --oneline --graph --decorate

[pull]
    rebase = true

[init]
    defaultBranch = main

{% if yadm.os == "Darwin" %}
[credential]
    helper = osxkeychain
{% endif %}

{% if yadm.os == "Linux" %}
[credential]
    helper = cache --timeout=3600
{% endif %}