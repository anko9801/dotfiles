# Ignore everything by default
/*

# Then whitelist what we want to track
!.config/
!.ssh/
!.gitignore
!.zshenv

# But still ignore sensitive/cache files within tracked directories
.config/*/cache/
.config/yadm/repo.git/
.ssh/id_*
.ssh/known_hosts
.ssh/authorized_keys

# System files
.DS_Store
*.swp
*~